export type WindowState = {
  x: number;
  y: number;
  width: number;
  height: number;
  prev_x: number;
  prev_y: number;
  visible: boolean;
  decorated: boolean;
  maximized: boolean;
  fullscreen: boolean;
};
